<x-app-layout><div class="space-y-6 p-6"><div><p class="text-sm font-semibold text-purple-700">Campus Registrar</p><h1 class="text-2xl font-bold">Beneficiary Verification Queue</h1></div><x-status-alerts/><div class="space-y-3">@forelse($batches as $batch)<a class="block rounded-lg bg-white p-5 shadow" href="{{ route('registrar.batches.show', $batch) }}"><strong>{{ $batch->masterlist->file_name }}</strong><span class="ml-2 text-sm text-gray-600">{{ $batch->records()->count() }} records</span></a>@empty<p class="rounded-lg bg-white p-5">No campus batches are waiting for verification.</p>@endforelse</div>{{ $batches->links() }}</div></x-app-layout>
